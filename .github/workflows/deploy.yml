name: deploy

on:
  push:
    branches: [ master ]
jobs:
  deploy:
    runs-on: [ self-hosted ]
    steps:
      - uses: actions/checkout@v2
      - run: ./scripts/prod/docker-compose-wrapper.sh down
      - run: ./scripts/prod/docker-compose-wrapper.sh up
